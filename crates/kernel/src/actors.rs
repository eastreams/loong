//! Intentionally empty after the Actix migration.
//!
//! The kernel's only actor, [`Kernel`](crate::Kernel), lives in the crate root
//! and runs on `loac`. This module stays as a named home for future
//! kernel-owned actors; do not use it to reintroduce a second actor runtime.
